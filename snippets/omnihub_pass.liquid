<!-- Omni Hubにより自動挿入された Snippet です。編集・削除はしないで下さい。 -->

<script defer src="{{ "omnihub_pass.js" | asset_url }}"></script>

<div
  id="omni-hub-pass"
  data-customer-id="{{ customer.id }}"
  data-shop-domain="{{shop.permanent_domain}}"
  data-enable-apple-wallet="{{enable_apple_wallet}}"
  data-enable-google-wallet="{{enable_google_wallet}}"
  data-disable-webview="{{disable_webview}}"
>
</div>
